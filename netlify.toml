[build]
  command = "pip install -r requirements.txt"
  publish = "."

[dev]
  command = "flask run --host=0.0.0.0 --port=8889"
  targetPort = 8889
  
[build.environment]
  FLASK_APP = "app.py"  # замените на имя вашего главного файла
  FLASK_DEBUG = "1"
